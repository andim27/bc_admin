var elements = {};$(document).ajaxComplete(function(){	// запомнить все поля и значения	$('input[type=text]').each(function(){		elements[$(this).attr('id')] = $(this).val();	})	// проверка	$('input[type=text]').focusout(function() {	//console.log(elements);		if ($(this).val() != elements[$(this).attr('id')] ) {			// отсылка лога			$.ajax({			 			  url: "http://" + window.location.hostname + "/admin-logs/log/",			  data: { 	page_id: location.href,							log_type_id:"4",							changes:'#' + $(this).attr('id') + ' was `' + elements[$(this).attr('id')] + '` changed to `' + $(this).val() + '`' 						},			  type: "GET"			})						//замена значения			elements[$(this).attr('id')] = $(this).val();		}	});	});